FROM python:3.12-slim

WORKDIR /app

COPY requirements/ /tmp/requirements/
RUN pip install --no-cache-dir -r /tmp/requirements/analysis.txt

COPY src/ /app/src/

ENV PYTHONPATH=/app/src

RUN mkdir -p /app/logs

CMD ["python", "-u", "-m", "analysis.main"]
